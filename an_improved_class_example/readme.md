This is an improved version of a_complete_class_example.
It uses dynamic arrays. Dellocates before exit.
It can read files of varying length.
input filename can be supplied as argv[1].
It throws error messages if no input file is provided or the input file cannot be opened.
It uses the extern construct to allow global variables to be shared between .cpp files.
